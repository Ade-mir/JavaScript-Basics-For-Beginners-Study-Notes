let numbers = [1, 2, 3, 4];
let another = numbers;

// solution 1
// numbers = [];

// Solution 2 (THE BEST ONE)
// numbers.length = 0;

// Solution 3
// numbers.splice(0, numbers.length);

// Solution 4
// while (numbers.length > 0)
//     numbers.pop();

console.log(numbers);
console.log(another);
