const numbers = [1, 2, 3, 4];

// Removing element on the end
// const last = numbers.pop();

// Removing element in the beginning
// const first = numbers.shift();

// Removing element in the middle
numbers.splice(2, 2);
console.log(numbers);
